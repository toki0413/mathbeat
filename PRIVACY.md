# MathBeat 隐私政策

**生效日期：** 2026-07-24

MathBeat（以下简称"本应用"）是一款将数学与音乐深度融合的教育游戏。我们高度重视用户隐私，尤其是当本应用可能被未成年学生使用时。本政策说明本应用如何处理用户数据。

## 1. 数据收集范围

本应用是一款**本地运行**的数学音乐教育游戏。所有用户数据——包括游戏进度、学习记录、错题历史（`wrongAnswerHistory`）、成就解锁状态、每日挑战打卡、作曲台作品、个性化学习目标、设置项等——**仅存储在用户本地浏览器的 `localStorage` 与 `IndexedDB` 中，不上传到任何服务器**。

具体存储位置：

- `localStorage`：游戏主状态（`mathbeat_state`）、会话标识（`mathbeat_session_id`）、应用版本（`mathbeat_app_version`）等键值。
- `IndexedDB`（数据库名 `mathbeat_db`）：科学之声作品（`mathbeat_science_compositions`）、存档影子备份（`mathbeat_state_prev`）与降级备份（`mathbeat_state_fallback`）。

本应用不要求注册账号，不收集姓名、邮箱、电话、学校、班级等任何个人身份信息。

## 2. 无远端上报

**默认情况下，本应用不向任何远端服务器上报数据。** 错误信息仅保存在本地内存环形缓冲区（最多 50 条），供开发者面板查看，关闭页面即丢失。

错误上报功能默认关闭。仅当用户**明确调用** `setRemoteEndpoint(url)` 配置了远端端点后，错误信息才会通过 `fetch` 异步发送至该地址。即便启用，发送前也会对敏感字段脱敏：

- **URL**：仅保留路径部分，剔除查询字符串（`record.url.split('?')[0]`）。
- **User-Agent**：截断为首个 token 且最长 40 字符（`userAgent.split(' ')[0].slice(0, 40)`）。

Web Vitals（LCP/CLS/INP）采集同样仅在配置了远端端点时，于页面隐藏时一次性上报，优先使用 `navigator.sendBeacon`，全部失败静默处理，不影响主流程。

## 3. 儿童隐私

本应用面向教育场景，可能被 13 岁以下儿童使用。我们遵守《儿童在线隐私保护法》（COPPA）与《儿童个人信息网络保护规定》：

- **不收集儿童个人身份信息**：无姓名、邮箱、电话、住址等字段。
- **不进行行为分析上报**：所有学习数据（错题、打卡、目标完成情况）仅本地存储，不外传。
- **不展示个性化广告**：本应用不含任何广告。
- **不使用第三方追踪 SDK**：无 Google Analytics、Facebook Pixel、Firebase 等分析或归因服务。

教师可在课堂中分发本应用而无需担心学生隐私泄露。

## 4. 数据控制权

用户对自己的数据拥有完全控制权：

- **一键清除**：通过应用内"设置 > 清除学习数据"可调用 `purgeUserData()`，一次性删除 `localStorage` 与 `IndexedDB` 中的全部用户数据（`mathbeat_state`、`mathbeat_state_prev`、`mathbeat_state_fallback`、`mathbeat_science_compositions`）并重置为初始状态。
- **重置进度**：通过"设置 > 重置进度"可重置游戏状态为默认值（保留应用本身）。
- **浏览器清除**：用户也可通过浏览器"清除站点数据"功能删除所有本地存储。
- **导出备份**：通过"设置 > 导出"调用 `exportGameData()` 可获取自己数据的 JSON 副本（`mathbeat_save.json`），建议定期导出以防本地数据丢失。
- **导入恢复**：通过"设置 > 导入"可从 JSON 备份恢复，导入数据会经过严格 schema 校验（含原型污染防御）。

## 5. 第三方服务

本应用**不使用任何第三方分析、广告或追踪服务**。

- **音频**：全部由 Web Audio API 实时本地合成，无外部音频样本、无流媒体请求。
- **字体**：使用系统默认字体栈，不加载第三方网络字体。
- **图标与 SVG**：项目原创或基于 MIT 兼容许可（详见 `THIRD_PARTY_NOTICES.md`）。
- **网络请求**：除用户主动配置的错误上报端点外，本应用不发起任何外部网络请求。

## 6. Cookie

**本应用不使用 Cookie。**

## 7. 数据安全

- 所有导入本应用的外部数据（存档、分享码、自定义关卡）均经过手写 schema 校验，拒绝 `__proto__`/`constructor`/`prototype` 等原型污染危险键，并限制字符串与数组长度以防 OOM。
- 存档损坏时，应用会尝试从 IndexedDB 影子备份恢复，避免进度静默丢失。
- 教师面板鉴权采用 Fail-Closed 策略：鉴权异常时禁止进入，避免越权访问。

## 8. 联系方式

如对本隐私政策有任何疑问，请通过项目仓库提交 Issue。

## 9. 政策更新

本政策可能随应用演进更新。重大变更将在应用内通知用户。继续使用本应用即视为接受更新后的政策；若不同意，用户可随时清除本地数据并停止使用。

---

本政策最后更新于 2026-07-24。
